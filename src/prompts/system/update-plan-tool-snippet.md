<!-- Usage: Added as the promptSnippet in workflow_update_plan tool metadata when the extension registers the tool. -->
Prepare an editable plan directory, then validate and finalize its JSON metadata and Markdown as an immutable version
