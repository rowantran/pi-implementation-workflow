- Run `npm run test:command-flow` with a local workflow fixture modeled on the FRED discussion: four explicitly agreed changes are accepted, the lock-release type suggestion remains proposed until agreed, and the helper rewrite is dismissed with the keep-as-is decision. Reloading the session retains those distinctions without reading the old transcript as the source of truth.
- In the same command-flow harness, invoke `/workflow-implement` after review. It starts a fresh implementation session in the same worktree, opens no request editor, and supplies the saved followups, exact approved plan, clarifications, latest report, pending IDs, and recheck IDs. Proposed and dismissed items are not selected for implementation.
- Run repeated-implementation cases with no followups, a partial original implementation, a partially finished followup list, and an entirely finished list. The handoff instructs the agent to finish missing work, verify already-satisfied work without duplicating it, and make no artificial changes when nothing remains. The original approval commit and approved plan version stay unchanged.
- Run the manual-request command cases: `/workflow-revise` still preserves editor cancellation and accepted text, but starts the shared implementation phase. Its submitted request can be saved as accepted followups before code changes. Existing manual uncommitted files and the checked-out stack tip are preserved.
- Run storage and artifact-Git tests against real temporary Git repositories. Adding, accepting, editing, dismissing, and recording implementation survive process restart; their commits contain only allowed workflow artifacts and preserve unrelated staged content. Original plan snapshots and completed reports remain byte-for-byte unchanged.
- Run invalid-write and concurrency cases in storage/command-flow tests. Duplicate or reused slugs, missing prerequisites, dependency cycles, unsafe paths, acceptance of work with unaccepted prerequisites, and stale concurrent writes are rejected without replacing the previous valid record. A simulated Git-commit failure retains the saved followup, reports the incomplete commit, and succeeds on retry without adding a duplicate.
- Run implementation-evidence cases with real Git commits. A recorded completion at unchanged code and scope is not pending; an already implemented manual fix can be recorded without a new code commit. Changed requirements, later code edits, a revert, or a rebase require rechecking old evidence. Failed verification never produces an implementation record. Artifact-only commits do not invalidate it.
- Run `npm run test:review` using its injected review-agent runner and local Git fixtures. The generated report contains every original change and every accepted followup, including implemented followups, exactly once. All reviewer prompts receive the accepted amendments. The testing reviewer receives original and followup criteria with identifiable sources; proposed and dismissed suggestions are excluded from required review scope.
- In review tests, accept a followup without changing code and request another review. The prior report is not reused, and a full review includes the new requirement. A later code-only change under the same scope permits incremental review. Recording implementation alone leaves the requirement fingerprint and reusable report unchanged.
- In review failure cases, change accepted scope or repository code while agents run. No completed report is published from mixed inputs. Retrying an interrupted same-input review reuses valid finished agent outputs. A current followup review with an unresolved verdict or blocking/warning concern produces recheck work, not a false completed state.
- Run `npm run test:dashboard` and `npm run test:ui`. Guided view, Full document, dependency links, and review sections distinguish original changes from followups; proposed/dismissed items do not look approved, and implementation evidence does not look like a passed review. Old plan versions and reports show their original scope. An accepted scope change visibly marks the saved report stale; a completion-only change does not.
- Run session-resume, prompt, and configuration tests. The interactive reviewer can call only permitted followup mutations and cannot write code or mark implementation complete. Isolated review agents have no followup mutation tool. Resumed legacy revision sessions receive unified implementation instructions, including followup paths; explicit implementing model settings win over a warned legacy revising fallback.
- Run legacy storage/dashboard cases with metadata version 6, plan schema version 1, no `followups.json`, and an existing version 3 review. The workflow remains readable without rewriting its plan or history; the next review writes a current-format report through a full review.
- Run `npm test` after adding these cases to the existing local harnesses. Tests use real workflow persistence and Git operations with controlled Pi, model-result, and GitHub fixtures; no infrastructure deployment or live pull request is required to verify the workflow orchestration.
