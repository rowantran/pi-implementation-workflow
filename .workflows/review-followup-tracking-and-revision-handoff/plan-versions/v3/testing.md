- Run `npm run test:command-flow` with a review-session fixture that calls the existing `workflow_update_plan` prepare operation, edits followup JSON/Markdown files with native write/edit, and finalizes. The new followup appears in the next numbered plan snapshot; no dedicated followup tool or separate followup store is created.
- In that fixture, model the FRED discussion: four explicitly agreed changes are accepted, the lock-release type suggestion stays proposed until agreed, and the helper rewrite is dismissed with the keep-as-is decision. Reloading the session retains those distinctions from the finalized plan files without needing the old conversation as the source of truth.
- Run plan-storage and write-guard tests. Review can edit only followup definition files and permitted manifest fields in working-plan; it cannot modify code, original plan files, implementation evidence, metadata, or published snapshots. Bypassing the native guard by modifying a test draft directly still fails finalization if the original baseline changed. The previous valid snapshot and approval pointer remain unchanged.
- Run dependency and validation cases. Duplicate or reused slugs, deleted published followup IDs, missing prerequisites, invalid decision citations, dependency/amendment cycles, accepted work referencing unaccepted prerequisites, unsafe links, and unexpected fields/files are rejected. A proposal cannot become implementation scope merely by finalizing it.
- Run concurrent draft cases. Preparing preserves unsaved edits, including invalid ones; an outdated expectedBaseVersion or a changed draft during publication fails without replacing the latest snapshot. `/workflow-implement` and `/workflow-review` refuse an unfinalized draft rather than silently leaving its followups behind.
- Invoke `/workflow-implement` after review in the command-flow harness. It opens no request editor and starts a fresh implementation session in the same worktree with the exact baseline/current snapshot paths, clarifications, latest report, and every active original/followup ID grouped as assess, pending, recheck, or complete. Proposed/dismissed items are not selected; unassigned plan-wide review/testing obligations remain visible. Existing manual code edits and the stack tip are preserved.
- Run first and repeated implementation cases with no followups. The first implementer can prepare, write progress files for original changes, and finalize before any review or followup exists. After a partial run, a fresh session distinguishes completed originals from pending or unassessed originals. After a fully recorded run, another session sees all originals complete without manufacturing edits or changing approval.
- Run a mixed-scope case containing one completed original, one unfinished original, one completed followup, and one unfinished accepted followup. The shared work list preserves both completed items and selects remaining work from both groups. Completed per-change records do not suppress unresolved plan-wide tests or holistic concerns.
- Run implementation evidence cases against real temporary Git repositories. The implementer writes implementation.json under original and accepted followup directories using the same schema, then finalizes a progress-only version. Frozen original metadata and Markdown remain byte-for-byte unchanged. Dirty code, incorrect content commits, wrong change/whole-scope fingerprints on newly written evidence, and edits to requirements are rejected. Already satisfied changes can be recorded without a new code commit; changed code, a revert, or a rebase require rechecking old evidence. Artifact-only commits do not.
- Start with completed original records, then accept an unrelated followup without changing code. The originals remain recorded complete while the new followup needs assessment. Amend one original requirement instead: that change and affected dependents need rechecking, while unrelated originals retain valid completion. Previously completed records are not reset solely because the whole-plan fingerprint changed.
- Run artifact-Git tests. Finalization creates a snapshot but no Git commit. The next workflow handoff/review/cleanup commits finalized history without consuming unrelated staged changes. A simulated artifact-commit failure retains the published snapshot and blocks handoff; retry commits it without inventing a duplicate followup or deleting history.
- Run `npm run test:review` with its injected agent runner and local Git fixtures. Reports include every original and accepted followup once, including implemented followups. All reviewers receive accepted amendments and exact immutable source paths. The testing reviewer receives original and followup criteria with identifiable sources; proposed/dismissed suggestions are not required scope.
- In review tests, accept a followup at the same code HEAD and request another review. The old report is not reused; a full review covers the new scope. Later code-only changes permit same-scope incremental review. A format-only conversion or evidence-only plan version leaves the requirement fingerprint and reusable current-format report unchanged.
- Change accepted scope or repository code while review agents run. The operation publishes no mixed-input report. Same-input retries reuse valid completed agent outputs. A current unresolved verdict or associated blocking/warning concern produces recheck work for an original or followup change rather than a false completed state.
- Run `npm run test:dashboard` and `npm run test:ui`. Guided view, Full document, version comparisons, dependency links, and review sections show the same progress states and evidence for originals and followups, with remaining-work totals covering both groups. The current version and original baseline are both identified; historical versions keep their own scope and progress. Only requirement changes make an otherwise current report stale.
- Run command, session-resume, prompt, and configuration tests. `/workflow-revise` is absent from registered commands, autocomplete, cards, and guidance; there is no request editor, alias, or active revision phase. Legacy serialized revision sessions resume with implementation behavior. A legacy models.revising configuration produces a specific migration message instead of silently falling back. Isolated review agents cannot edit plan drafts or finalize followups.
- Run legacy storage/dashboard cases with metadata version 6, schema version 1 plans, no progress files, and version 3 reports. Old artifacts remain readable and unchanged; original changes start Unassessed rather than automatically complete or known missing. After checking existing implementation/review evidence, record already satisfied originals in a new version without a code change. The first post-approval draft upgrades independently, and the next review writes a current-format report through a full review.
- Run `npm test` after adding these cases to the existing harnesses. Tests use real workflow persistence and Git with controlled Pi, model-result, and GitHub fixtures. No infrastructure deployment or live pull request is required to verify the workflow orchestration.
