- Run `npm run test:command-flow` with a review-session fixture using workflow_update_plan prepare, native edits to followup JSON/Markdown, and finalize. The followup appears in the next plan snapshot with implemented: false; no dedicated followup tool, separate task store, or implementation.json is created.
- Model the FRED discussion in that fixture: agreed changes are accepted, the lock-release type suggestion remains proposed until agreed, and the helper rewrite is dismissed. Resuming the session retains those decisions from finalized files without requiring the old conversation as implementation context.
- Run storage and write-guard tests. Version 2 requires a boolean implemented field on original and followup metadata. Implementation may change only that field within the draft; attempts to alter title, dependencies, explanation, decision, or amendment data fail finalization. Original requirement Markdown remains byte-identical, original metadata apart from the flag stays equal, and historical snapshots never change.
- Run first and repeated implementation cases before any followups exist. The first implementer can update originals' implemented fields through prepare/edit/finalize. A new session receives false originals as remaining work and true originals as already reported implemented. A mixed original/followup case selects false items from both groups and excludes proposed/dismissed work.
- Set implemented: true without a new code commit or evidence document, then finalize. The flag is saved as an assessment, not verified completion. Changing code, rebasing, adding an unrelated followup, or generating a failed review does not automatically clear it. An implementer can explicitly set it back to false. Partial runs preserve their true/false values across resume.
- Run a review case where a change marked true receives a failing verdict. The report still includes that change and leaves its flag unchanged. After the user agrees to a correction, the review assistant can publish a new false followup; a user-agreed edit to an existing followup explicitly resets that followup to false. Review cannot mark any item true.
- Invoke `/workflow-implement` after review. It opens no editor and starts a fresh session in the same worktree with exact baseline/current paths, clarifications, latest review coverage, remaining IDs, and reported-implemented IDs. Existing manual code edits and the stack tip survive. When all active flags are true, the kickoff does not manufacture work or claim that tests/review passed.
- Run dependency and validation cases. Duplicate or reused slugs, deleted followup IDs, unknown prerequisites, invalid decision citations, dependency/amendment cycles, accepted work requiring unaccepted items, unsafe links, and unexpected fields/files are rejected without replacing the latest valid snapshot.
- Run concurrency and handoff cases. Prepare preserves unsaved edits; stale expectedBaseVersion values and draft changes during finalization fail safely. Implementation/review handoff refuses unfinalized draft differences rather than silently omitting followups or flag edits.
- Run artifact-Git tests against real temporary repositories. Finalization creates a plan snapshot but no Git commit and does not certify clean code. Handoff/review/cleanup commits the saved history without consuming unrelated staged changes. A failed artifact commit retains the snapshot, blocks handoff, and succeeds on retry without duplicating or deleting history. Existing review delivery checks still reject dirty or unpushed code regardless of flags.
- Run `npm run test:review` with the injected runner. It reviews every original and accepted followup regardless of implemented, receives accepted amendments and immutable source paths, and checks original/followup testing groups. Accepting a followup at the same code HEAD requires a full review; later same-scope code changes permit incremental review. Toggling implemented or converting the plan format alone does not change the requirement fingerprint or invalidate a current-format report.
- Change requirements or code while review agents run. No mixed-input report is published. Retrying identical inputs reuses valid finished agent outputs. Neither review generation nor cache reuse writes implemented flags.
- Run `npm run test:dashboard` and `npm run test:ui`. Both original and followup items show marked/unmarked status, counts exclude unaccepted work, and independent verdicts remain separate. A true item can visibly have a failing review. Historical versions retain their own flag values; there are no evidence panels or automatic recheck states.
- Run command, prompt, resume, and configuration tests. `/workflow-revise` is absent from commands, autocomplete, cards, and guidance. There is no alias or request editor. Legacy serialized revision sessions resume with implementation behavior; legacy models.revising configuration receives a migration message. Isolated review agents cannot edit drafts or flags.
- Run legacy cases with version 1 plans and version 3 reports. Old files stay readable and unchanged; absent implemented fields normalize to false, without asserting that code is missing. Prepare upgrades the editable copy with explicit false fields. The implementer can inspect existing code and mark old work true without reimplementing it; the next review writes a current-format report through a full review.
- Run `npm test` after adding these cases to the existing harnesses. Use real workflow persistence and Git with controlled Pi, model-result, and GitHub fixtures. No deployment or live pull request is required to verify the orchestration.
